#ifndef _OTHER_FUNCTIONS_H_
#define _OTHER_FUNCTIONS_H_

#include <vector>
#include <utility>

using namespace std;

bool sort_pair(const pair<pair<int, int> , double> &, const pair<pair<int, int>, double> &);

bool sort_pair2(const pair<double, pair<int, int> > &, const pair<double, pair<int, int> > &);

bool compare(pair<int, int> &, pair<int, int> &);

#endif
